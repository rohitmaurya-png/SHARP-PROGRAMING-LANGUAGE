mod std;

import std.io;

/**
 * Created by Rohit Maurya on 11/19/2020.
 *
 *
 */
stable class _nil_ {
    static instance : _nil_ = new _nil_ { };
        set value -> { } // prevent the user from setting a new value

    public def to_string() := new string("nil");
}
